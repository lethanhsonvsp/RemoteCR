@page "/"
@rendermode InteractiveServer
@using RemoteCR.Services.Can
@inject CanStateContainer State
@inject DeltaChargerCommandService dccs
@implements IDisposable

<style>
    /* ================= HEADER ================= */
    .status-dot {
        display: inline-block;
        width: 14px;
        height: 14px;
        border-radius: 50%;
        margin-right: 6px;
    }

        .status-dot.online {
            background: #28a745;
            box-shadow: 0 0 8px #28a745;
        }

        .status-dot.offline {
            background: #dc3545;
            box-shadow: 0 0 8px #dc3545;
        }

    /* ================= GRID ================= */
    .gui {
        display: grid;
        grid-template-columns: 0.9fr 0.9fr 2.2fr; /* 👈 thu gọn cột 1,2 */
        gap: 10px;
    }

    /* ================= CARD ================= */
    .card {
        background: #fff;
        border-radius: 8px;
        padding: 10px 12px;
        box-shadow: 0 0 8px rgba(0,0,0,.12);
        margin-bottom: 10px;
    }

    .title {
        font-weight: 600;
        font-size: .9rem;
        margin-bottom: 6px;
    }

    /* ================= ROW ================= */
    .row {
        display: grid;
        grid-template-columns: 130px 1fr;
        font-size: .8rem;
        align-items: center;
    }

        .row div {
            text-align: right;
            white-space: nowrap;
        }

    /* ================= STATUS / FAULT ================= */
    .chk {
        display: grid;
        grid-template-columns: 16px 1fr;
        font-size: .8rem;
        align-items: center;
    }

    .on {
        color: #28a745;
        font-weight: 600;
    }

    .warn {
        color: #ffc107;
        font-weight: 600;
    }

    .fault {
        color: #dc3545;
        font-weight: 600;
    }

    .off {
        color: #999;
    }

    /* ================= CAN ================= */
    .can-filter {
        display: flex;
        flex-wrap: wrap;
        gap: 8px;
        font-size: .75rem;
        margin-bottom: 6px;
    }

    .can-split {
        display: grid;
        grid-template-columns: 1fr 1fr;
        gap: 8px;
    }

    .can-log {
        height: 420px;
        background: #111;
        color: #0f0;
        font-family: monospace;
        font-size: .75rem;
        padding: 4px 6px;      /* đều 2 bên */
        overflow-y: auto;
        border-radius: 6px;
        text-align: left;      /* đảm bảo không bị inherit lệch */
    }
        .can-log.tx {
            color: #0ff;
            background: #0b1a1a;
        }

    .can-line {
        white-space: pre;
        text-align: left;      /* ép trái */
        width: 100%;           /* chiếm full chiều ngang */
        display: block;        /* không inline */
        padding: 0;
        margin: 0;
    }

</style>

<h3>Delta 1kW Wireless Charger</h3>
<h5>
    <span class="status-dot @(State.IsConnected ? "online" : "offline")"></span>
    @(State.IsConnected ? "Connected" : "Disconnected")
</h5>

<div class="gui">

    <!-- ================= COLUMN 1 ================= -->
    <div>
        <div class="card">
            <div class="title">Set Point Control</div>

            <div class="row">
                <span>Current (A)</span>
                <div><input type="number" @bind="current" style="width:70px" /></div>
            </div>

            <div class="row">
                <span>Voltage (V)</span>
                <div><input type="number" @bind="voltage" style="width:70px" /></div>
            </div>

            <div class="row">
                <span>X</span>
                <div><input type="number" @bind="x" style="width:70px" /></div>
            </div>

            <div class="mt-2">
                <button class="btn btn-success btn-sm" @onclick="SendOnceOn">Once ON</button>
                <button class="btn btn-secondary btn-sm ms-1" @onclick="SendOnceOff">Once OFF</button>
            </div>

            <div class="mt-1">
                <button class="btn btn-primary btn-sm" @onclick="StartLoopOn">Start Loop</button>
                <button class="btn btn-primary btn-sm" @onclick="AcquireCanControlAsync">AcquireCanControlAsync</button>
                <button class="btn btn-warning btn-sm ms-1" @onclick="StartLoopOff">Stop Loop</button>
                <button class="btn btn-danger btn-sm ms-1" @onclick="Clear">ClearFault</button>
            </div>
        </div>

        <div class="card">
            <div class="title">Measurements</div>

            <div class="row"><span>Voltage</span><div>@State.Voltage.ToString("F1") V</div></div>
            <div class="row"><span>Current</span><div>@State.Current.ToString("F1") A</div></div>
            <div class="row"><span>Power</span><div>@State.Power.ToString("F1") W</div></div>

            <hr />

            <div class="row"><span>AC Voltage</span><div>@State.AcVoltage.ToString("F1") V</div></div>
            <div class="row"><span>AC Current</span><div>@State.AcCurrent.ToString("F1") A</div></div>
            <div class="row"><span>AC Power</span><div>@State.AcPower.ToString("F1") W</div></div>
            <div class="row"><span>AC Freq</span><div>@State.AcFreq Hz</div></div>

            <hr />

            <div class="row"><span>Gap</span><div>@State.Gap mm</div></div>
            <div class="row"><span>Temp Pri</span><div>@State.PriTemp °C</div></div>
            <div class="row"><span>Temp Sec</span><div>@State.SecTemp °C</div></div>
        </div>
    </div>

    <!-- ================= COLUMN 2 ================= -->
    <div>
        <div class="card">
            <div class="title">Status</div>
            @Chk("WirelessComm", State.GUI_WirelessComm)
            @Chk("StatusPower", State.GUI_StatusPower)
            @Chk("PowerDerating", State.GUI_PowerDerating, "warn")
            @Chk("Fault", State.GUI_Fault, "fault")
        </div>

        <div class="card">
            <div class="title">Faults</div>
            @Fault("OCP_SW", ChargerFault.OCP)
            @Fault("OTP_SW", ChargerFault.OTP_Primary)
            @Fault("OVP_HW", ChargerFault.OVP)
            @Fault("OVP_SW", ChargerFault.OVP)
            @Fault("WD_Comm", ChargerFault.CommTimeout)
            @Fault("WD_Reset", ChargerFault.SafetyStop)
            @Fault("OverGap", ChargerFault.GapTooHigh, "warn")
        </div>

        <!-- Block 7 -->
        <div class="card">
            <div class="title">Version</div>
            <div class="row"><span>SEC</span><div>@State.RevMCU1</div></div>
            <div class="row"><span>IPT</span><div>@State.RevMCU2</div></div>
            <div class="row"><span>PFC</span><div>@State.RevDSP</div></div>
        </div>
    </div>

    <!-- ================= COLUMN 3 ================= -->
    <div>
        <div class="card">
            <div class="title">CAN Monitor</div>

            <div class="can-filter">
                @foreach (var id in KnownIds)
                {
                    <label>
                        <input type="checkbox"
                               checked="@EnabledIds.Contains(id)"
                               @onchange="e => ToggleId(id, e)" />
                        @id.ToString("X3")
                    </label>
                }
            </div>

            <div class="can-split">
                <!-- RX -->
                <div>
                    <div class="title">RX</div>
                    <div class="can-log">
                        @foreach (var f in RxFrames)
                        {
                            <pre class="can-line">can0  @f.Id.ToString("X3")  [@f.Dlc]  @f.Hex</pre>
                        }
                    </div>
                </div>

                <!-- TX -->
                <div>
                    <div class="title">TX</div>
                    <div class="can-log tx">
                        @foreach (var f in TxFrames)
                        {
                            <pre class="can-line">can0  @f.Id.ToString("X3")  [@f.Dlc]  @f.Hex</pre>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

@code {
    double voltage = 55;
    double current = 10;
    uint x = 1;

    RenderFragment Chk(string t, bool v, string c = "on")
        => @<div class="chk @(v? c : "off")">
        <input type="checkbox" checked="@v" />@t
    </div>;

    RenderFragment Fault(string t, ChargerFault f, string c = "fault")
        => @<div class="chk @(State.FaultFlags.HasFlag(f) ? c : "off")">
        <input type="checkbox" checked="@State.FaultFlags.HasFlag(f)" />@t
    </div>;

    void SendOnceOn() => dccs.SendOnce(x,voltage, current, true);
    void SendOnceOff() { dccs.SendOnce(x, voltage, current, false); dccs.StopLoop(); }
    void StartLoopOn() => dccs.StartLoop(x, voltage, current, true);
    void AcquireCanControlAsync() => dccs.AcquireCanControlAsync(x, voltage, current);
    void StartLoopOff() => dccs.StopLoop();
    void Clear() => dccs.Send190(x,voltage, current, true, true);

    HashSet<uint> EnabledIds = new() { 0x311 };

    uint[] KnownIds = { 0x190, 0x311, 0x321, 0x3C1, 0x3E1, 0x5F1, 0x771 };

    IEnumerable<CanFrame> RxFrames =>
        State.GetRxSnapshot()
            .Where(f => EnabledIds.Contains(f.Id & 0x7FF))
            .TakeLast(150)
            .Reverse();   // 👈 MỚI NHẤT Ở TRÊN

    IEnumerable<CanFrame> TxFrames =>
        State.GetTxSnapshot()
            .Where(f => EnabledIds.Contains(f.Id & 0x7FF))
            .TakeLast(150)
            .Reverse();   // 👈 MỚI NHẤT Ở TRÊN

    void ToggleId(uint id, ChangeEventArgs e)
    {
        if ((bool)e.Value!) EnabledIds.Add(id);
        else EnabledIds.Remove(id);
    }

    protected override void OnInitialized()
    {
        State.OnChange += OnStateChanged;
    }

    private void OnStateChanged()
    {
        _ = InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        State.OnChange -= OnStateChanged;
    }
}
